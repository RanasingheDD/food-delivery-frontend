# The name of your pipeline visible in GitHub's UI
name: CI/CD Pipeline

# 1. Define the Triggers (When should this run?)
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# 2. Define the Stages (What should it do?)
jobs:
  # Continuous Integration Job
  build-and-test:
    runs-on: [self-hosted]

    steps:
      # Step 1: Clone your repository code onto the runner
      - name: Checkout Code
        uses: actions/checkout@v4

      # Step 2: Set up the programming language environment
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      # Step 3: Install application dependencies
      - name: Install Dependencies
        run: npm ci

      - name: Static Code Analysis
        uses: sonarsource/sonarqube-scan-action@master
        env: 
          SONAR_TOKEN: ${{secrets.SONAR_TOKEN}}
          SONAR_URL: ${{secrets.SONAR_TOKEN}}


  # Continuous Deployment Job
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build-and-test # Only runs if the test job passes successfully
  #   if: github.ref == 'refs/heads/main' # Only runs on the main branch

  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4

  #     - name: Deploy Application
  #       # Replace this echo with actual cloud provider commands (e.g., AWS, Vercel, Docker)
  #       run: echo "Deploying production assets..." 
